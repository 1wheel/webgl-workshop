#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ndpack-image/bin/ndpack-image.js" "$@"
  ret=$?
else 
  node  "$basedir/../ndpack-image/bin/ndpack-image.js" "$@"
  ret=$?
fi
exit $ret
